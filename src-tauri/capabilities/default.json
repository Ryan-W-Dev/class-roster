{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Core permissions required by the Class Roster main window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
